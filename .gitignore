# Secrets
.env

# Python venv + bytecode
.venv/
__pycache__/

# macOS
.DS_Store

# Claude Code session state
.claude/

# Raw satellite data (multi-GB; regenerable via scripts/fetch_project.py).
# Manifest.json sidecars are tracked so users see what was fetched.
data/raw/sentinel2/**/*.tif
data/raw/sentinel2/**/*.tiff
data/raw/sentinel2/**/*.jp2
data/raw/hansen/*.tif
